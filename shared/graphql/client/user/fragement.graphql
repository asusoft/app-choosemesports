fragment FullUser on User {
    id
    name
    login
    email
    role
    avatar {
        ...Media
    }
}

fragment SimpleUser on User {
    id
    name
    login
    avatar {
        ...Media
    }
}
