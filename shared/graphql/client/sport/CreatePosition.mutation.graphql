mutation CreatePosition($input: PositionIn!) {
  createPosition(input: $input) {
        __typename
        ...on Position {
            ...FullPosition
        }
        ... on BaseError {
            status
        }
    }
}