query GetSportPositions ($sportID: ID!){
    getSportPositions (sportID: $sportID) { 
        __typename
        ... on BaseError {
            status
        }
        ... on PositionList {
            ...PositionList
        }
    }
}