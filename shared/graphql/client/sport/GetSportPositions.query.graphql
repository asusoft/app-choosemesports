query GetSportPositions ($id: ID!){
    getSportPositions (id: $id) { 
        __typename
        ... on BaseError {
            status
        }
        ... on PositionList {
            ...PositionList
        }
    }
}