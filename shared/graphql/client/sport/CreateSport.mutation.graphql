mutation CreateSport($input: SportIn!) {
  createSport(input: $input) {
        __typename
        ...on Sport {
            ...FullSport
        }
        ... on BaseError {
            status
        }
    }
}