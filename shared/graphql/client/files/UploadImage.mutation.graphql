mutation UploadImage($file: Upload!){
    uploadImage(file: $file) {
        __typename
        ... on BaseError {
            status
        }
        ... on File {
            id
            path
            type
        }
    }
}