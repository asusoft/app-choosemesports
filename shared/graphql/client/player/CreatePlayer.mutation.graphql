mutation CreatePlayer($input: PlayerIn!) {
    createPlayer(input: $input) {
        __typename
        ... on AuthUser {
            user {
                ...FullUser
            }
            token
        }
        ... on ErrorWithFields {
            status
            fields
        }
    }
}